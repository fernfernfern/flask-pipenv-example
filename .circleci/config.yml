version: 2.1

orbs:
  po: fernfernfern/python-orb@dev:aplha

workflows:
  test:
    jobs:
      - po/python-job:
          install-deps: true
          test-steps: |
            pipenv run python -m pytest
