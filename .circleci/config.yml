version: 2.1

orbs:
  po: fernfernfern/python-orb@0.0.4

workflows:
  test:
    jobs:
      - po/python-job:
          install-deps: true
          test-steps: |
            pipenv run python -m pytest
